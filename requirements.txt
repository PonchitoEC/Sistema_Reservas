# Framework principal
Django==5.2.15

# Servidor WSGI para producción (requerido por Render)
gunicorn==22.0.0

# PostgreSQL - psycopg3 (compatible con Python 3.14)
psycopg[binary]==3.2.4

# Parseo de DATABASE_URL (Render lo inyecta automáticamente)
dj-database-url==2.2.0

# Archivos estáticos en producción sin servidor externo
whitenoise==6.7.0

# Generación de facturas/comprobantes PDF en memoria
reportlab==4.2.5
